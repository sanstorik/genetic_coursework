<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/colorBackgroundMain</item>
        <item name="android:actionBarStyle">@style/BarTheme</item>
    </style>

    <style name="MyDialog" parent="Theme.AppCompat.Dialog.Alert">
    </style>

    <style name="BarTheme" parent="@style/AlertDialog.AppCompat.Light">
        <item name="android:background">@color/colorButtons</item>
    </style>

</resources>
